# Create runner in terraform

create project, runner, enablement, token

save token to `.env`

template token into ps1 file

modify `gcp.tf`` to use the token in gcp vm to register runner

ssh to gcp, check token in startup logs

